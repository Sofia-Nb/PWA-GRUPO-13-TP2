import { n as __require, t as __commonJSMin } from "./chunk-CRPLlQ3x.js";
//#region node_modules/react-i18next/icu.macro.js
var require_icu_macro = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	var { createMacro } = __require("babel-plugin-macros");
	module.exports = createMacro(ICUMacro);
	function ICUMacro({ references, state, babel }) {
		const { Trans = [], Plural = [], Select = [], SelectOrdinal = [], number = [], date = [], select = [], selectOrdinal = [], plural = [], time = [] } = references;
		addNeededImports(state, babel, references);
		[...Plural, ...SelectOrdinal].forEach((referencePath) => {
			if (referencePath.parentPath.type === "JSXOpeningElement") pluralAsJSX(referencePath.parentPath, {
				attributes: referencePath.parentPath.get("attributes"),
				children: referencePath.parentPath.parentPath.get("children")
			}, babel);
		});
		Select.forEach((referencePath) => {
			if (referencePath.parentPath.type === "JSXOpeningElement") selectAsJSX(referencePath.parentPath, {
				attributes: referencePath.parentPath.get("attributes"),
				children: referencePath.parentPath.parentPath.get("children")
			}, babel);
		});
		Trans.forEach((referencePath) => {
			if (referencePath.parentPath.type === "JSXOpeningElement") transAsJSX(referencePath.parentPath, {
				attributes: referencePath.parentPath.get("attributes"),
				children: referencePath.parentPath.parentPath.get("children")
			}, babel);
		});
		Object.entries({
			number,
			date,
			time,
			select,
			plural,
			selectOrdinal
		}).forEach(([name, node]) => {
			node.forEach((item) => {
				let f = item.parentPath;
				while (f) {
					if (babel.types.isJSXElement(f.node)) {
						const { openingElement } = f.node;
						const elementName = openingElement.name;
						if (!babel.types.isJSXMemberExpression(elementName) && (elementName.name === "IcuTrans" || elementName.name === "Trans")) return;
					}
					f = f.parentPath;
				}
				const { loc } = item.node;
				if (!loc) throw new Error(`"${name}\`\`" can only be used inside <Trans> in "unknown file"`);
				throw new Error(`"${name}\`\`" can only be used inside <Trans> in "${loc.filename}" on line ${loc.start.line}`);
			});
		});
	}
	function pluralAsJSX(parentPath, { attributes }, babel) {
		const t = babel.types;
		const toObjectProperty = (name, value) => t.objectProperty(t.identifier(name), t.identifier(name), false, !value);
		const nodeName = parentPath.node.name.name.toLocaleLowerCase();
		const existingValuesAttribute = findAttribute("values", attributes);
		const existingValues = existingValuesAttribute ? existingValuesAttribute.node.value.expression.properties : [];
		let componentStartIndex = 0;
		const extracted = attributes.reduce((mem, attr) => {
			if (attr.node.name.name === "i18nKey") mem.attributesToCopy.push(attr.node);
			else if (attr.node.name.name === "count") {
				let exprName = attr.node.value.expression.name;
				if (!exprName) exprName = "count";
				if (exprName === "count") mem.attributesToCopy.push(attr.node);
				else mem.values.unshift(toObjectProperty(exprName));
				mem.defaults = `{${exprName}, ${nodeName}, ${mem.defaults}`;
			} else if (attr.node.name.name === "values") {} else if (attr.node.value.type === "StringLiteral") {
				let pluralForm = attr.node.name.name;
				if (pluralForm.indexOf("$") === 0) pluralForm = pluralForm.replace("$", "=");
				mem.defaults = `${mem.defaults} ${pluralForm} {${attr.node.value.value}}`;
			} else if (attr.node.value.type === "JSXExpressionContainer") {
				const thisTrans = processTrans(attr.node.value.expression.children || [], babel, componentStartIndex);
				let pluralForm = attr.node.name.name;
				if (pluralForm.indexOf("$") === 0) pluralForm = pluralForm.replace("$", "=");
				mem.defaults = `${mem.defaults} ${pluralForm} {${thisTrans.defaults}}`;
				mem.components = mem.components.concat(thisTrans.components);
				componentStartIndex += thisTrans.components.length;
			}
			return mem;
		}, {
			attributesToCopy: [],
			values: existingValues,
			components: [],
			defaults: ""
		});
		parentPath.replaceWith(buildTransElement(extracted, extracted.attributesToCopy, t, true));
	}
	function selectAsJSX(parentPath, { attributes }, babel) {
		const t = babel.types;
		const toObjectProperty = (name, value) => t.objectProperty(t.identifier(name), t.identifier(name), false, !value);
		const existingValuesAttribute = findAttribute("values", attributes);
		const existingValues = existingValuesAttribute ? existingValuesAttribute.node.value.expression.properties : [];
		let componentStartIndex = 0;
		const extracted = attributes.reduce((mem, attr) => {
			if (attr.node.name.name === "i18nKey") mem.attributesToCopy.push(attr.node);
			else if (attr.node.name.name === "switch") {
				let exprName = attr.node.value.expression.name;
				if (!exprName) {
					exprName = "selectKey";
					mem.values.unshift(t.objectProperty(t.identifier(exprName), attr.node.value.expression));
				} else mem.values.unshift(toObjectProperty(exprName));
				mem.defaults = `{${exprName}, select, ${mem.defaults}`;
			} else if (attr.node.name.name === "values") {} else if (attr.node.value.type === "StringLiteral") mem.defaults = `${mem.defaults} ${attr.node.name.name} {${attr.node.value.value}}`;
			else if (attr.node.value.type === "JSXExpressionContainer") {
				const thisTrans = processTrans(attr.node.value.expression.children || [], babel, componentStartIndex);
				mem.defaults = `${mem.defaults} ${attr.node.name.name} {${thisTrans.defaults}}`;
				mem.components = mem.components.concat(thisTrans.components);
				componentStartIndex += thisTrans.components.length;
			}
			return mem;
		}, {
			attributesToCopy: [],
			values: existingValues,
			components: [],
			defaults: ""
		});
		parentPath.replaceWith(buildTransElement(extracted, extracted.attributesToCopy, t, true));
	}
	function transAsJSX(parentPath, { attributes, children }, babel) {
		const defaultsAttr = findAttribute("defaults", attributes);
		const contentAttr = findAttribute("content", attributes);
		const componentsAttr = findAttribute("components", attributes);
		const parseDefaults = defaultsAttr && !contentAttr && !componentsAttr;
		let extracted;
		if (parseDefaults) {
			const defaultsExpression = defaultsAttr.node.value.value;
			const parsed = babel.parse(`<>${defaultsExpression}</>`, {
				presets: ["@babel/react"],
				filename: babel.state?.filename || "unknown"
			}).program.body[0].expression.children;
			extracted = processTrans(parsed, babel);
		} else extracted = processTrans(children, babel);
		let clonedAttributes = cloneExistingAttributes(attributes);
		if (parseDefaults) clonedAttributes = clonedAttributes.filter((node) => node.name.name !== "defaults");
		(children.length ? children[0].parentPath : parentPath).replaceWith(buildTransElement(extracted, clonedAttributes, babel.types, false, !!children.length));
	}
	function buildTransElement(extracted, finalAttributes, t, closeDefaults = false, wasElementWithChildren = false) {
		const nodeName = t.jSXIdentifier("IcuTrans");
		if (closeDefaults) extracted.defaults += "}";
		const contentDeclarations = extracted.components.map((component) => jsxElementToDeclaration(component, t));
		const content = t.arrayExpression(contentDeclarations);
		const values = t.objectExpression(extracted.values);
		if (!attributeExistsAlready("defaultTranslation", finalAttributes)) if (extracted.defaults.includes("\"")) finalAttributes.push(t.jSXAttribute(t.jSXIdentifier("defaultTranslation"), t.jSXExpressionContainer(t.stringLiteral(extracted.defaults))));
		else finalAttributes.push(t.jSXAttribute(t.jSXIdentifier("defaultTranslation"), t.stringLiteral(extracted.defaults)));
		if (!attributeExistsAlready("content", finalAttributes)) finalAttributes.push(t.jSXAttribute(t.jSXIdentifier("content"), t.jSXExpressionContainer(content)));
		if (!attributeExistsAlready("values", finalAttributes)) finalAttributes.push(t.jSXAttribute(t.jSXIdentifier("values"), t.jSXExpressionContainer(values)));
		const openElement = t.jSXOpeningElement(nodeName, finalAttributes, true);
		if (!wasElementWithChildren) return openElement;
		return t.jSXElement(openElement, null, [], true);
	}
	/**
	* Convert a JSX element to a declaration object: { type: Component, props: {...} }
	*/
	function jsxElementToDeclaration(jsxElement, t) {
		if (!jsxElement || !jsxElement.openingElement) {
			console.error("Invalid JSXElement passed to jsxElementToDeclaration:", jsxElement);
			return t.objectExpression([t.objectProperty(t.identifier("type"), t.stringLiteral("div"))]);
		}
		const elementName = jsxElement.openingElement.name;
		let typeValue;
		if (t.isJSXIdentifier(elementName)) if (elementName.name.toLowerCase() === elementName.name) typeValue = t.stringLiteral(elementName.name);
		else typeValue = t.identifier(elementName.name);
		else if (t.isJSXMemberExpression(elementName)) {
			const objectName = t.isJSXIdentifier(elementName.object) ? elementName.object.name : "unknown";
			const propertyName = elementName.property.name;
			typeValue = t.memberExpression(t.identifier(objectName), t.identifier(propertyName));
		} else typeValue = t.stringLiteral("div");
		const properties = [t.objectProperty(t.identifier("type"), typeValue)];
		const propsProperties = [];
		jsxElement.openingElement.attributes.forEach((attr) => {
			if (t.isJSXAttribute(attr)) {
				const propName = t.isJSXIdentifier(attr.name) ? attr.name.name : String(attr.name);
				let propValue;
				if (attr.value === null) propValue = t.booleanLiteral(true);
				else if (t.isStringLiteral(attr.value)) propValue = attr.value;
				else if (t.isJSXExpressionContainer(attr.value)) propValue = attr.value.expression;
				else propValue = t.nullLiteral();
				const propKey = /^[a-zA-Z_$][a-zA-Z0-9_$]*$/.test(propName) ? t.identifier(propName) : t.stringLiteral(propName);
				propsProperties.push(t.objectProperty(propKey, propValue));
			} else if (t.isJSXSpreadAttribute(attr)) propsProperties.push(t.spreadElement(attr.argument));
		});
		if (jsxElement.children && jsxElement.children.length > 0) {
			const childDeclarations = jsxElement.children.filter((child) => t.isJSXElement(child)).map((child) => jsxElementToDeclaration(child, t));
			if (childDeclarations.length > 0) propsProperties.push(t.objectProperty(t.identifier("children"), t.arrayExpression(childDeclarations)));
		}
		if (propsProperties.length > 0) properties.push(t.objectProperty(t.identifier("props"), t.objectExpression(propsProperties)));
		return t.objectExpression(properties);
	}
	function cloneExistingAttributes(attributes) {
		return attributes.reduce((mem, attr) => {
			const node = attr.node ? attr.node : attr;
			if (node.type === "JSXAttribute" && node.name && node.name.name === "defaults") return mem;
			if (node.type === "JSXAttribute" && node.name && node.name.name === "components") return mem;
			mem.push(node);
			return mem;
		}, []);
	}
	function findAttribute(name, attributes) {
		return attributes.find((child) => {
			return (child.node ? child.node : child).name.name === name;
		});
	}
	function attributeExistsAlready(name, attributes) {
		return !!findAttribute(name, attributes);
	}
	function processTrans(children, babel, componentStartIndex = 0) {
		const res = {};
		res.defaults = mergeChildren(children, babel, componentStartIndex);
		res.components = getComponents(children, babel);
		res.values = getValues(children, babel);
		return res;
	}
	var leadingNewLineAndWhitespace = /^\n\s+/g;
	var trailingNewLineAndWhitespace = /\n\s+$/g;
	function trimIndent(text) {
		return text.replace(leadingNewLineAndWhitespace, "").replace(trailingNewLineAndWhitespace, "");
	}
	/**
	* add comma-delimited expressions like `{ val, number }`
	*/
	function mergeCommaExpressions(ele) {
		if (ele.expression && ele.expression.expressions) return `{${ele.expression.expressions.reduce((m, i) => {
			m.push(i.name || i.value);
			return m;
		}, []).join(", ")}}`;
		return "";
	}
	/**
	* this is for supporting complex icu type interpolations
	* date`${variable}` and number`{${varName}, ::percent}`
	* also, plural`{${count}, one { ... } other { ... }}
	*/
	function mergeTaggedTemplateExpressions(ele, componentFoundIndex, t, babel) {
		if (t.isTaggedTemplateExpression(ele.expression)) {
			const [, text, index] = getTextAndInterpolatedVariables(ele.expression.tag.name, ele.expression, componentFoundIndex, babel);
			return [text, index];
		}
		return ["", componentFoundIndex];
	}
	function mergeChildren(children, babel, componentStartIndex = 0) {
		const t = babel.types;
		let componentFoundIndex = componentStartIndex;
		return children.reduce((mem, child) => {
			const ele = child.node ? child.node : child;
			let result = mem;
			if (t.isJSXText(ele) && ele.value) result += trimIndent(ele.value);
			if (ele.expression && ele.expression.value) result += ele.expression.value;
			if (ele.expression && ele.expression.name) result += `{${ele.expression.name}}`;
			result += mergeCommaExpressions(ele);
			const [nextText, newIndex] = mergeTaggedTemplateExpressions(ele, componentFoundIndex, t, babel);
			result += nextText;
			componentFoundIndex = newIndex;
			if (t.isJSXElement(ele)) {
				result += `<${componentFoundIndex}>${mergeChildren(ele.children, babel)}</${componentFoundIndex}>`;
				componentFoundIndex += 1;
			}
			return result;
		}, "");
	}
	var extractTaggedTemplateValues = (ele, babel, toObjectProperty) => {
		if (ele.expression && ele.expression.type === "TaggedTemplateExpression") {
			const [variables] = getTextAndInterpolatedVariables(ele.expression.tag.name, ele.expression, 0, babel);
			return variables.map((vari) => toObjectProperty(vari));
		}
		return [];
	};
	/**
	* Extract the names of interpolated value as object properties to pass to IcuTrans
	*/
	function getValues(children, babel) {
		const t = babel.types;
		const toObjectProperty = (name, value) => t.objectProperty(t.identifier(name), t.identifier(name), false, !value);
		return children.reduce((mem, child) => {
			const ele = child.node ? child.node : child;
			let result = mem;
			if (ele.expression && ele.expression.name) mem.push(toObjectProperty(ele.expression.name));
			if (ele.expression && ele.expression.expressions) result.push(toObjectProperty(ele.expression.expressions[0].name || ele.expression.expressions[0].value));
			if (ele.expression && ele.expression.properties) result = result.concat(ele.expression.properties);
			result = result.concat(extractTaggedTemplateValues(ele, babel, toObjectProperty));
			if (t.isJSXElement(ele)) result = result.concat(getValues(ele.children, babel));
			return result;
		}, []);
	}
	/**
	* Common logic for adding a child element of Trans to the list of components to hydrate the translation
	* @param {JSXElement} jsxElement
	* @param {JSXElement[]} mem
	*/
	var processJSXElement = (jsxElement, mem, t) => {
		const clone = t.clone(jsxElement);
		clone.children = clone.children.reduce((clonedMem, clonedChild) => {
			const clonedEle = clonedChild.node ? clonedChild.node : clonedChild;
			if (clonedEle.expression && clonedEle.expression.expressions) clonedEle.expression.expressions = [clonedEle.expression.expressions[0]];
			clonedMem.push(clonedChild);
			return clonedMem;
		}, []);
		mem.push(jsxElement);
	};
	/**
	* Extract the React components to pass to IcuTrans as content
	*/
	function getComponents(children, babel) {
		const t = babel.types;
		return children.reduce((mem, child) => {
			const ele = child.node ? child.node : child;
			if (t.isJSXExpressionContainer(ele)) {
				if (t.isTaggedTemplateExpression(ele.expression)) ele.expression.quasi.expressions.forEach((expr) => {
					if (t.isTaggedTemplateExpression(expr) && expr.quasi.expressions.length) mem.push(...getComponents(expr.quasi.expressions, babel));
					if (!t.isJSXElement(expr)) return;
					processJSXElement(expr, mem, t);
				});
			}
			if (t.isJSXElement(ele)) processJSXElement(ele, mem, t);
			return mem;
		}, []);
	}
	var icuInterpolators = [
		"date",
		"time",
		"number",
		"plural",
		"select",
		"selectOrdinal"
	];
	var importsToAdd = ["IcuTrans"];
	/**
	* helper split out of addNeededImports to make codeclimate happy
	*
	* This does the work of amending an existing import from "react-i18next", or
	* creating a new one if it doesn't exist
	*/
	function addImports(state, existingImport, allImportsToAdd, t) {
		if (existingImport) allImportsToAdd.forEach((name) => {
			if (existingImport.specifiers.findIndex((specifier) => specifier.imported && specifier.imported.name === name) === -1) existingImport.specifiers.push(t.importSpecifier(t.identifier(name), t.identifier(name)));
		});
		else state.file.path.node.body.unshift(t.importDeclaration(allImportsToAdd.map((name) => t.importSpecifier(t.identifier(name), t.identifier(name))), t.stringLiteral("react-i18next")));
	}
	/**
	* Add `import { IcuTrans, number, date, <etc.> } from "react-i18next"` as needed
	*/
	function addNeededImports(state, babel, references) {
		const t = babel.types;
		const existingImport = state.file.path.node.body.find((importNode) => t.isImportDeclaration(importNode) && importNode.source.value === "react-i18next");
		const usedRefs = Object.keys(references).filter((importName) => {
			if (!icuInterpolators.includes(importName)) return false;
			return references[importName].length;
		});
		addImports(state, existingImport, importsToAdd.concat(usedRefs), t);
	}
	/**
	* iterate over a node detected inside a tagged template literal
	*
	* This is a helper function for `extractVariableNamesFromQuasiNodes` defined below
	*
	* this is called using reduce as a way of tricking what would be `.map()`
	* into passing in the parameters needed to both modify `componentFoundIndex`,
	* `stringOutput`, and `interpolatedVariableNames`
	* and to pass in the dependencies babel, and type. Type is the template type.
	* For "date``" the type will be `date`. for "number``" the type is `number`, etc.
	*/
	var extractNestedTemplatesAndComponents = ({ componentFoundIndex: lastIndex, babel, stringOutput, type, interpolatedVariableNames }, node) => {
		let componentFoundIndex = lastIndex;
		if (node.type === "JSXElement") {
			const subText = `<${componentFoundIndex}>${mergeChildren(node.children, babel)}</${componentFoundIndex}>`;
			componentFoundIndex += 1;
			stringOutput.push(subText);
		} else if (node.type === "TaggedTemplateExpression") {
			const [variableNames, childText, newIndex] = getTextAndInterpolatedVariables(babel.types.isIdentifier(node.tag) && node.tag.name ? node.tag.name : "unknown", node, componentFoundIndex, babel);
			interpolatedVariableNames.push(...variableNames);
			componentFoundIndex = newIndex;
			stringOutput.push(childText);
		} else if (node.type === "Identifier") {
			const nodeName = node.name || "unknown";
			stringOutput.push(`${nodeName}, ${type}`);
		} else if (node.type === "TemplateElement") {
			const cookedValue = node.value.cooked || "";
			stringOutput.push(cookedValue.replace(/\s+/g, " "));
		}
		return {
			componentFoundIndex,
			babel,
			stringOutput,
			type,
			interpolatedVariableNames
		};
	};
	/**
	* filter the list of nodes within a tagged template literal to the 4 types we can process,
	* and ignore anything else.
	*
	* this is a helper function for `extractVariableNamesFromQuasiNodes`
	*/
	var filterNodes = (node) => {
		if (node.type === "Identifier") return node.name;
		if (node.type === "JSXElement" || node.type === "TaggedTemplateExpression") return true;
		if (node.type === "TemplateElement") return node.value.cooked;
		return false;
	};
	var errorOnInvalidQuasiNodes = (primaryNode) => {
		const noInterpolationError = !primaryNode.quasi.expressions.length;
		const wrongOrderError = primaryNode.quasi.quasis[0].value.raw.length;
		const tagName = primaryNode.tag.name || "unknown";
		const message = `${tagName} argument must be interpolated ${noInterpolationError ? "in" : "at the beginning of"} "${tagName}\`\`" in "${primaryNode.loc?.filename}" on line ${primaryNode.loc?.start.line}`;
		if (noInterpolationError || wrongOrderError) throw new Error(message);
	};
	var extractNodeVariableNames = (varNode, babel) => {
		const interpolatedVariableNames = [];
		if (varNode.type === "JSXElement") interpolatedVariableNames.push(...getValues(varNode.children, babel).map((value) => {
			if (babel.types.isIdentifier(value.value)) return value.value.name;
			return String(value.value);
		}));
		else if (varNode.type === "Identifier") interpolatedVariableNames.push(varNode.name);
		return interpolatedVariableNames;
	};
	var extractVariableNamesFromQuasiNodes = (primaryNode, babel) => {
		errorOnInvalidQuasiNodes(primaryNode);
		const text = [];
		const interpolatedVariableNames = [];
		primaryNode.quasi.expressions.forEach((varNode) => {
			if (!babel.types.isIdentifier(varNode) && !babel.types.isTaggedTemplateExpression(varNode) && !babel.types.isJSXElement(varNode)) {
				const tagName = primaryNode.tag.name || "unknown";
				throw new Error(`Must pass a variable, not an expression to "${tagName}\`\`" in "${primaryNode.loc?.filename}" on line ${primaryNode.loc?.start.line}`);
			}
			text.push(varNode);
			interpolatedVariableNames.push(...extractNodeVariableNames(varNode, babel));
		});
		primaryNode.quasi.quasis.forEach((quasiNode) => {
			text.push(quasiNode);
		});
		return {
			text,
			interpolatedVariableNames
		};
	};
	var throwOnInvalidType = (type, primaryNode) => {
		if (!icuInterpolators.includes(type)) throw new Error(`Unsupported tagged template literal "${type}", must be one of date, time, number, plural, select, selectOrdinal in "${primaryNode.loc?.filename}" on line ${primaryNode.loc?.start.line}`);
	};
	/**
	* Retrieve the new text to use, and any interpolated variables
	*
	* This is used to process tagged template literals like date`${variable}` and number`${num}, ::percent`
	*
	* for the data example, it will return text of `{variable, date}` with a variable of `variable`
	* for the number example, it will return text of `{num, number, ::percent}` with a variable of `num`
	* @param {string} type the name of the tagged template (`date`, `number`, `plural`, etc. - any valid complex ICU type)
	* @param {TaggedTemplateExpression} primaryNode the template expression node
	* @param {int} index starting index number of components to be used for interpolations like <0>
	* @param {*} babel
	*/
	function getTextAndInterpolatedVariables(type, primaryNode, index, babel) {
		throwOnInvalidType(type, primaryNode);
		const componentFoundIndex = index;
		const { text, interpolatedVariableNames } = extractVariableNamesFromQuasiNodes(primaryNode, babel);
		const { stringOutput, componentFoundIndex: newIndex } = text.filter(filterNodes).sort((a, b) => {
			const aStart = a.start != null ? a.start : null;
			const bStart = b.start != null ? b.start : null;
			if (aStart != null && bStart != null && aStart > bStart) return 1;
			return -1;
		}).reduce(extractNestedTemplatesAndComponents, {
			babel,
			componentFoundIndex,
			stringOutput: [],
			type,
			interpolatedVariableNames
		});
		return [
			interpolatedVariableNames,
			`{${stringOutput.join("")}}`,
			newIndex
		];
	}
}));
//#endregion
export default require_icu_macro();

//# sourceMappingURL=react-i18next_icu__macro.js.map